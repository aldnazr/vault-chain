import 'package:flutter/material.dart';

class NftTabBar extends StatelessWidget {
  const NftTabBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('NFT Page'));
  }
}
